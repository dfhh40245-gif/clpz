[pytest]
markers =
    slow: marks tests as slow (e2e pipeline tests, ~20s each)
    integration: marks tests requiring the full server (but not network)
    network: marks tests requiring external network access
    e2e: marks end-to-end tests that exercise the full pipeline
